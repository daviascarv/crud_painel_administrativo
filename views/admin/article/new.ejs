<%- include('../../partials/header.ejs') %>
<%- include('../../partials/navbar.ejs') %>

<body>
    <div class="container">
        <hr>
        <div class="card">
            <div class="card-header">
                <h2>Novo Artigo</h2>
            </div>
            <div class="card-body">
        <hr>
        <form method="POST" action="/article/save">
            <input class="form-control" type="text" name="title" placeholder="Defina o título da artigo">
            <textarea name="body" placeholder="Escreva o artigo aqui" class="form-control"></textarea>
            <label for="">Categoria</label>
            <select name="category" class="form-control" id="">
                <% categories.forEach((category) => { %>
                    <option value="<%= category.id %>"><%= category.title %></option>
                <% }) %>
            </select>
            <br>
            <button class="btn btn-success">Cadastre-se</button>
        </form>
        </div>
        </div>
    </div>
</body>

<%- include('../../partials/footer.ejs') %>